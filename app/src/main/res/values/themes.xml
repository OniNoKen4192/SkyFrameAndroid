<resources>
    <style name="Theme.SkyFrame" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0a1018</item>
        <item name="android:navigationBarColor">#0a1018</item>
        <!-- enableEdgeToEdge() forces transparent system bars; this fills the
             area behind them with the HUD base color so there's no mismatched
             strip above the status bar / below the nav bar. -->
        <item name="android:windowBackground">#0a1018</item>
    </style>
</resources>
